# A rudimentary unix shell implementation. Requires library libreadline (used for maintaining history).

1. sudo apt install libreadline-dev
2. gcc shell_1.2.c -lreadline
3. a) ./a.out
 or 
   b) ./a.out cmd.txt